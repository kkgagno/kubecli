<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aggregated Compliance Report</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .container { max-width: 1200px; margin: auto; }
        .header { background-color: #f2f2f2; padding: 15px; border-bottom: 1px solid #ddd; margin-bottom: 20px; }
        .status-fail { color: #dc3545; font-weight: bold; }
        .host-section { margin-bottom: 30px; border: 1px solid #ccc; padding: 20px; border-radius: 5px; }
        .table { margin-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="text-center">Aggregated Compliance Report</h1>
            <p class="text-center">Generated On: {{ ansible_date_time.iso8601 }}</p>
        </div>

        {% for report_item in reports %}
            <div class="host-section">
                <h2>Host: <strong>{{ report_item.hostname }}</strong> - <span class="status-fail">{{ report_item.report.summary['failed-count'] }} Failures</span></h2>
                <table class="table table-striped table-bordered">
                    <thead class="table-dark">
                        <tr>
                            <th>Title</th>
                            <th>Resource Type</th>
                            <th>Resource ID</th>
                            <th>Property</th>
                            <th>Expected</th>
                            <th>Found</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for result in report_item.report.results %}
                            {% if result.result == 1 %}
                                <tr>
                                    <td>{{ result.title }}</td>
                                    <td>{{ result['resource-type'] | capitalize }}</td>
                                    <td>{{ result['resource-id'] }}</td>
                                    <td>{{ result.property }}</td>
                                    <td>{{ result.expected | default(['N/A']) | join(', ') }}</td>
                                    <td>{{ result.found | default(['N/A']) | join(', ') }}</td>
                                </tr>
                            {% endif %}
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endfor %}

        <div class="footer text-center mt-4">
            <p>Report generated by Ansible and Goss.</p>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
